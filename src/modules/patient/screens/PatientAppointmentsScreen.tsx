import React from 'react';
import { PlaceholderScreen } from '../../../shared/ui/templates/PlaceholderScreen';

export function PatientAppointmentsScreen() {
  return (
    <PlaceholderScreen
      title="Appointments"
      subtitle="Bookings"
      description="Patients will view, book, reschedule, cancel, and track appointment approvals here."
    />
  );
}